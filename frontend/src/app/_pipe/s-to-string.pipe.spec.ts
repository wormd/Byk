import { SToStringPipe } from './s-to-string.pipe';

describe('SToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
